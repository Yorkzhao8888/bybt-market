# DESIGN.md — X-Market 五域集市系统

## 产品气质与意象
- **锚点意象**：现代都市交易枢纽 · 深夜仍亮着五色灯笼的赛博市集。五条街道（物资 / 人力 / 空间 / 技术 / 门店产能）在中央广场交汇，交易、点货、履约在摊位上同屏发生。
- **气质关键词**：市集烟火气 x 交易终端的秩序感。信息密集但层级清晰，像交易员在摊位上清点货物的凝视。
- 遮住产品名仍能认出：五色域条 + 前后两层“摊位卡”（前店售卖面 / 后厂履约面）。

## 配色方案
- **纸色基调** `#F5F2EB`（暖调宣纸），墨色正文 `#17181D`，界框细线 `#E4DED2`。
- **五域五色**（导航与卡片域标的可识别符）：
  - E 物资 → 赭金 `#C27A1B`
  - H 人力 → 橘红 `#E4572E`
  - Y 空间 → 松石 `#17A290`
  - T 技术 → 靛蓝 `#4A5FD5`
  - DE 门店 → 品红 `#D6366E`
- **双入口色**：Mall(C端) 用金 `#B8862B`；Market(B端) 用墨 `#17181D`。
- 硬影卡片 `box-shadow: 4px 4px 0 rgba(23,24,29,0.12)`，呼应摊位台的物理重量。

## 字体排版
- 标题：`Noto Serif SC, Songti SC, serif`（编辑部衬线，700 字重，负磅密集感）。
- 正文/UI：`PingFang SC, "Microsoft YaHei", system-ui, sans-serif`。
- 节奏：大标题用衬线大字，信息区用紧凑 sans，形成“招牌 vs 清单”的市集对比。

## 布局与组件规范
- **Booth 双层卡**：前店售卖面（亮纸卡 + 硬影）叠加后厂履约面（深墨后板 `#17181D`），可切换两层面板，视觉上前后两层错位堆叠。
- **域切换**：横向五色域条（每域一色块 + 域代码如 E/H/Y/T/DE），当前域高亮。
- **交易清单**：表格式、等宽数字、紧凑行距，营造交易终端感。

## 四类角色工作台主题色（X-MARKET-09）
- 主题色用于：顶栏激活态/侧栏激活/身份徽标/主按钮；全局浅米白 `#F5F2EB` + 炭黑 `#17181D` 不变；主题色为纯色块（禁渐变堆叠）。
- 客户工作台（XU/CU，/market 或 /mall，浏览引导型）：信任蓝 `#1D4ED8`，浅底 `#E8EEFB`，深字 `#1E40AF`。
- 供应商工作台（YU/EU/HU/TU，/supplier，业务操作型·左导航）：源头绿 `#15803D`，浅底 `#E8F5EC`，深字 `#166534`。
- 经营者工作台（DU 及执行帽，/operator，驾驶舱型·左导航+顶部 KPI）：活力橙 `#B45309`，浅底 `#FBF0E0`，深字 `#92400E`。
- 治理者工作台（V*M/VXM，/govern，管控型·左导航+全局统计）：治理紫 `#6D28D9`，浅底 `#F0E9FC`，深字 `#5B21B6`。
- 五域五色（E/H/Y/T/DE 域条与卡片段）继续生效，与工作台主题色分层共存：主题色管"身份"，域色管"业务"。
- 登录落点：CU→/mall、XU→/market、供给帽→/supplier、DU/执行帽→/operator、V*M/VXM→/govern；越权直访返回 403 兜底页。

## 动效与交互
- 转入：`cubic-bezier(0.2, 0.8, 0.2, 1)`，120–180ms，轻盈推进。
- hover：卡片硬影沉深一档 + 域色描边点亮；层级切换用推移过渡。
- 双入口 Logo 区：Mall 金色 / Market 墨色，悬停微抬升。

## 设计禁忌
- 禁用科技蓝 + 紫渐变堆叠卡片。
- 禁用全白无层次、玻璃拟态过重、圆角过度（本系统圆角 ≤ 12px）。
- 不作泛泛“高端感”留白，信息力求一次绕组清楚。

## DU 三端体验设计（X-MARKET-UE-01）
- **三端配合**：管理 PC（管·决策 /operator）+ 操作手机（办·执行 /operator/mobile）+ 现场看板（展·状态 /board，深墨大屏）。
- **状态彩色徽标**（pill+前缀圆点，全端统一）：已完成绿 `#16A34A` / 处理中·待报价灰棕 `#8a6d3b` / 待审批琥珀 `#D97706` / 待治理审批紫 `#6D28D9` / 履约中蓝 `#2563EB` / 越权 denied 红 `#DC2626`。
- **驾驶舱首屏**：欢迎区（时段问候+身份副信息）→ 待办分级条（待报价/待审批/待履约，红点强提醒，点击直达模块）→ 三张硬影数字卡 → 最近订单表。
- **货品陈列**：卡片式（域色块缩略+名称+单位+价格+标签），空态必给动作引导，不做干瘪空页。
- **手机作业端**：max-w-md 单手操作，大按钮（≥44px 触达），底部 Tab 四区（作业/订单/回执/我的）；履约回执内联展示 actor_user+actor_hat 双字段。
- **现场看板**：深墨底 `#17181D` 大字远观，只展示不提醒（待办数、履约进度、成交额、订单流）；10s 轮询与三端同源。